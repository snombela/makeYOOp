{{#if user.isBrand}}
<h1>My upload products</h1>
{{else}}
<h1>My Favorites products</h1>
{{/if}}

<div class="container-fluid">
  <div class="row">
    <div class="col-md-4">
      <div class="red">
        <form action="/profile" method="post" enctype="multipart/form-data">
        <img src="{{user.imgPath}}" alt="user image">
        <input type="file" name="photo">
        <button type="submit">Enviar</button>
        </form>

        <h2>{{user.name}}</h2>

      </div>
    </div>

    <div class="col-md-7 blue">
      <div class="blue">
      {{#each products}}
      <div class="container-fluid">
        <div class="row">
          <article class="col-md-8">
          <img src="{{this.image_link}}">
          <h5><a href="/products/{{this._id}}">{{this.name}}</a></h5>
          {{#unless ../user.isBrand}}
          <p><a href="/products/favorite/remove/{{this._id}}">remove favorite</a></p>
          {{/unless}}
          <br>
          </article>
        </div>
      </div>
      {{/each}}
    </div>
    </div>
  </div>
</div>

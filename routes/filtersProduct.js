function allBrands(products, key, value){
    let filter = []
    products.forEach(product => {
        if (filters.indexOf(product.filters) == -1) filter.push(product.filters)  
        filter.value 
    })};

    var filtered = products.filter(product => {
        return product["key"] == value 
    })

    





    function allBrands(brands){
        let brands = []
        products.forEach(product => {
            if (brands.indexOf(product.brand) == -1) brands.push(product.brand)   
        })};

